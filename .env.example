# Copy to .env and adjust if needed. docker-compose.yml reads these to
# configure both the postgres container and the api service's DB_* vars.
DB_NAME=avito_kitchen
DB_USER=avito_kitchen
DB_PASSWORD=avito_kitchen
